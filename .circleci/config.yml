version: 2

jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: docker build 3.16-r13b -t mosesliao/cocos2dx-docker:3.16-r13b
      - run: docker build -t mosesliao/cocos2dx-docker .

  deploy:
    machine: true
    steps:
      - run: curl -H "Content-Type: application/json" --data '{"docker_tag": "master"}' -X POST https://registry.hub.docker.com/u/mosesliao/cocos2dx-docker/trigger/$DOCKER_TRIGGER_TOKEN/

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
      - deploy:
          filters:
            branches:
              only: master
